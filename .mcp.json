{
  "mcpServers": {
    "egtsr": {
      "command": "python3",
      "args": ["-m", "mcp_server"],
      "env": {
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT:-.}",
        "EGTSR_HOME": "${CLAUDE_PLUGIN_DATA:-.egtsr}"
      }
    }
  }
}
